{
  "name": "radian-mcp-cloudflare",
  "version": "1.0.18",
  "description": "24/7 Hosted Model Context Protocol (MCP) Server for Radian with Server-Sent Events (SSE) & Streamlined Client-Side SQLite/D1 Hybrid Storage.",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init": "wrangler d1 execute sectograph-db --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240923.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.10"
  },
  "private": true
}
