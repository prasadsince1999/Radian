{
  "name": "sectograph-mcp-server",
  "version": "1.0.0",
  "description": "Cloudflare D1-backed remote MCP server for Sectograph, compatible with ChatGPT and Claude",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "d1:init": "wrangler d1 execute sectograph-db --file=schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240923.0",
    "typescript": "^5.5.4",
    "wrangler": "^3.78.10"
  },
  "private": true
}
